# HANDOFF

更新日: 2026-08-19

## やったこと

- iOS 26 の WebKit で、下固定メニューがスクロール中に本文と一緒に動く不具合へ対応した。
- document 全体ではなく `.page-shift` だけをスクロールさせ、`.tabbar` はその外側で `.page` 下端に固定する構成へ変更した。
- 新しいスクロール領域に合わせて、引っ張って更新の先頭判定とハイライト選択時の先頭スクロールを更新した。

## いまの状態

- 393 × 852 のモバイル表示で、本文を 720px スクロールしてもメニュー座標は `top: 789px / bottom: 838px` のまま変わらないことを確認済み。
- 最下部でもフッターとメニューの間に約29pxの余白があり、内容がメニューに隠れないことを確認済み。
- `npm run build`、`git diff --check`、変更箇所の ESLint、秘密情報パターン確認は成功。
- リポジトリ全体の `npm run lint` は、変更前の `HEAD` にもある既存14件（未使用変数、React refルール等）で失敗する。今回の変更による新規エラーはない。

## 次の一歩

- iPhoneのホーム画面PWAをいったん閉じて開き直し、「日程」で上下へ数回勢いよくスクロールして、ボトムメニューが画面下に留まることを実機確認する。
- 原因になったWebKit側の不具合は未解決: https://bugs.webkit.org/show_bug.cgi?id=312149

## 動作確認方法

```bash
npm run dev -- --host 127.0.0.1
npm run build
```

モバイル幅で `.page-shift` の `scrollTop` だけが増え、`window.scrollY` と `.tabbar` の画面座標が変わらないことを確認する。
